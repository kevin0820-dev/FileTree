import { FolderData } from "../types";

const myData:FolderData = {
  id: "1",
  name: "My Folder",
  isFolder: true,
  items: [{
    id: "2",
    name: "Transcriptions",
    isFolder: true,
    items: [],
  }],
};

export default myData;
